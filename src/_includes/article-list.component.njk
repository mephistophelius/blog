<ul class="article-list without-list-style">
  {% for article in articles %}
  <li>
    <time datetime="{{ post.date | date }}">{{ article.date | date }}</time>
    <span class="separator">:</span>
    <a href="{{ article.url | url }}">{{ article.data.title }}</a>
  </li>
  {% endfor %}
</ul>
